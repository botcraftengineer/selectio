# Отправка приветствий по номеру телефона

## Описание

Реализован fallback механизм для отправки приветственных сообщений кандидатам в Telegram. Если у кандидата отсутствует Telegram username, система автоматически попытается отправить сообщение по номеру телефона через добавление контакта.

## Как это работает

1. **Приоритет username**: Сначала система пытается отправить сообщение по Telegram username (если он указан)
2. **Fallback на телефон**: Если username отсутствует или отправка не удалась, система:
   - Импортирует контакт в Telegram по номеру телефона
   - Отправляет сообщение найденному пользователю
3. **Логирование**: Все попытки отправки логируются для отладки

## Изменённые файлы

### Backend (packages/tg-client)
- `src/client.ts` - добавлена функция `sendMessageByPhone()`
- `src/index.ts` - экспортирована новая функция

### Jobs (packages/jobs)
- `src/inngest/types.ts` - обновлена схема события `candidate/welcome`
- `src/inngest/send-candidate-welcome.ts` - добавлена логика fallback
- `src/inngest/send-candidate-welcome-batch.ts` - добавлена логика fallback для массовой отправки

### Frontend (apps/web)
- `src/actions/trigger.ts` - обновлена функция `triggerSendWelcome()` для передачи телефона
- `src/components/response/response-actions.tsx` - обновлён UI компонент
- `src/components/vacancy/response-row.tsx` - передача phone в компонент
- `src/components/vacancy/response-cards.tsx` - передача phone в компонент

## Требования

- Номер телефона должен быть в международном формате (начинаться с `+`)
- Пользователь с указанным номером должен быть зарегистрирован в Telegram
- У бота должны быть права на импорт контактов

## Примеры использования

```typescript
// Отправка по username
await sendMessageByUsername("username", "Привет!");

// Отправка по телефону
await sendMessageByPhone("+79991234567", "Привет!", "Иван");

// Автоматический fallback в Inngest функции
// Система сама выберет метод отправки
await inngest.send({
  name: "candidate/welcome",
  data: {
    responseId: "123",
    username: "user123", // опционально
    phone: "+79991234567", // опционально
  },
});
```

## Логи

Система выводит подробные логи:
- `📨 Попытка отправки по username: @username`
- `📞 Попытка отправки по номеру телефона: +7999...`
- `✅ Сообщение отправлено по username/телефону`
- `⚠️ Не удалось отправить по username, пробуем по телефону`
