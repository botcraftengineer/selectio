# MTProto Sessions - Важные особенности

## Проблема: Обновления не приходят

Если `dp.onNewMessage(handler)` не срабатывает, причина в том, что **другое устройство получает обновления**.

### Как работает MTProto

Telegram MTProto имеет ограничение: **только одна активная сессия может получать обновления (updates)**.

```
┌─────────────────────────────────────┐
│         Telegram Server             │
└─────────────────┬───────────────────┘
                  │
                  │ Updates идут только
                  │ на ОДНУ активную сессию
                  │
         ┌────────┴────────┐
         │                 │
    ┌────▼────┐      ┌────▼────┐
    │ Phone   │      │  Bot    │
    │ (active)│      │(waiting)│
    └─────────┘      └─────────┘
         ✅               ❌
    Получает         Не получает
    обновления       обновления
```

### Активная сессия

Активной считается сессия, которая:
1. Последней подключилась
2. Активно опрашивает сервер (long polling)
3. Не была завершена

Если на телефоне открыт Telegram → он активная сессия → бот не получает сообщения.

## Решение 1: Завершить другие сессии (Рекомендуется)

При запуске бота завершаем все другие сессии:

```typescript
await client.call({
  _: "auth.resetAuthorizations",
});
```

**Что происходит:**
- Все другие устройства (телефон, десктоп) выходят из аккаунта
- Бот становится единственной активной сессией
- Все обновления идут в бота

**Недостаток:**
- Пользователь не может использовать Telegram на других устройствах
- При каждом входе на телефоне нужно заново авторизоваться

**Когда использовать:**
- Для выделенных аккаунтов-ботов
- Когда не нужен доступ с других устройств

## Решение 2: Использовать отдельный аккаунт

Создайте отдельный Telegram аккаунт для бота:

```
Основной аккаунт: +79991234567 (для личного использования)
Аккаунт бота:     +79991234568 (только для бота)
```

**Преимущества:**
- ✅ Нет конфликтов с другими устройствами
- ✅ Можно использовать основной аккаунт на телефоне
- ✅ Четкое разделение

**Недостатки:**
- Нужен дополнительный номер телефона
- Нужно управлять двумя аккаунтами

## Решение 3: Использовать Bot API вместо MTProto

Bot API не имеет этой проблемы:

```typescript
import { Bot } from "grammy";

const bot = new Bot("BOT_TOKEN");
bot.on("message", (ctx) => {
  // Всегда получает обновления
});
```

**Преимущества:**
- ✅ Нет конфликтов с сессиями
- ✅ Проще в использовании
- ✅ Официальная поддержка

**Недостатки:**
- ❌ Меньше возможностей API
- ❌ Нельзя отправлять сообщения первым
- ❌ Ограничения rate limit

## Решение 4: Периодически забирать активность

Периодически "будить" бота, чтобы он стал активной сессией:

```typescript
// Каждые 5 минут делаем запрос
setInterval(async () => {
  try {
    await client.call({ _: "updates.getState" });
    console.log("🔄 Обновили активность сессии");
  } catch (error) {
    console.error("Ошибка обновления активности:", error);
  }
}, 5 * 60 * 1000);
```

**Проблема:**
- Не гарантирует получение всех обновлений
- Может быть задержка до 5 минут

## Наша реализация

Мы используем **Решение 1** - завершаем другие сессии при запуске:

```typescript
// В bot-manager.ts
await client.call({
  _: "auth.resetAuthorizations",
});
```

### Как это работает

1. Бот запускается
2. Завершает все другие сессии
3. Становится единственной активной сессией
4. Получает все обновления

### Логи

```
🚀 Запуск всех Telegram ботов...
📋 Найдено 1 сессий
🔄 Завершение других сессий для workspace ws_123...
✅ Другие сессии завершены для workspace ws_123
✅ Бот запущен для workspace ws_123: Иван Иванов (@ivan) [+79991234567]
👂 Слушаем входящие сообщения...
```

## Проверка активной сессии

### Через Telegram

1. Откройте Telegram на телефоне
2. Settings → Privacy and Security → Active Sessions
3. Увидите список всех активных сессий
4. После `auth.resetAuthorizations` останется только бот

### Через код

```typescript
const sessions = await client.call({
  _: "account.getAuthorizations",
});

console.log("Активные сессии:", sessions.authorizations.length);
for (const auth of sessions.authorizations) {
  console.log(`- ${auth.appName} (${auth.deviceModel})`);
}
```

## Troubleshooting

### Бот не получает сообщения

```bash
# 1. Проверить, что бот запущен
kubectl logs -f deployment/telegram-bots | grep "Слушаем"

# 2. Проверить активные сессии в Telegram
# Settings → Privacy and Security → Active Sessions

# 3. Перезапустить бота (завершит другие сессии)
kubectl rollout restart deployment/telegram-bots
```

### Телефон постоянно выходит из аккаунта

Это нормально, если используется `auth.resetAuthorizations`.

**Решения:**
- Используйте отдельный аккаунт для бота
- Или не используйте телефон с этим аккаунтом

### Задержка в получении сообщений

```typescript
// Проверить, что dispatcher запущен
const dp = Dispatcher.for(client);
dp.onNewMessage(handler);

// Убедиться, что клиент подключен
console.log("Connected:", client.isConnected());
```

## Best Practices

### 1. Используйте выделенные аккаунты

```
❌ Плохо: Использовать личный аккаунт для бота
✅ Хорошо: Создать отдельный аккаунт для бота
```

### 2. Логируйте завершение сессий

```typescript
console.log("🔄 Завершение других сессий...");
await client.call({ _: "auth.resetAuthorizations" });
console.log("✅ Другие сессии завершены");
```

### 3. Обрабатывайте ошибки

```typescript
try {
  await client.call({ _: "auth.resetAuthorizations" });
} catch (error) {
  console.warn("⚠️ Не удалось завершить сессии:", error);
  // Продолжаем работу
}
```

### 4. Документируйте поведение

Предупредите пользователей, что:
- Другие устройства будут выходить из аккаунта
- Нужно использовать выделенный аккаунт для бота
- Нельзя одновременно использовать аккаунт на телефоне и в боте

## Альтернатива: Webhook вместо Long Polling

Если используете Bot API (не MTProto), можно использовать webhook:

```typescript
// Telegram отправляет обновления на ваш сервер
app.post("/webhook", async (req, res) => {
  const update = req.body;
  await handleUpdate(update);
  res.json({ ok: true });
});
```

**Преимущества:**
- Нет проблем с сессиями
- Масштабируется (несколько серверов)
- Telegram гарантирует доставку

**Недостатки:**
- Только для Bot API (не MTProto)
- Нужен публичный HTTPS endpoint

## Заключение

**Проблема:** MTProto позволяет только одной сессии получать обновления  
**Решение:** Завершаем другие сессии при запуске бота  
**Рекомендация:** Используйте выделенный аккаунт для бота

Реализовано в `bot-manager.ts` ✅
